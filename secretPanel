def secretPanel(p):
    word = ""
    for c in p:
        if c == " ":
            word += " "
        else:
            word += "_"
    return word

print(secretPanel(""))
print(secretPanel("hello"))
print(secretPanel("Good afternoon"))
